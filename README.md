# Веб-сервис планирования туристических маршрутов Казани

Система для планирования и описания туристических маршрутов в городе Казань на Django и React.

## Возможности

- 🔐 Регистрация и аутентификация пользователей (JWT)
- 🗺️ Генерация маршрутов с помощью LLM (Perplexity) и алгоритмов
- 📍 Управление достопримечательностями
- 🗺️ Интерактивные карты с визуализацией маршрутов
- 📊 Аналитика и статистика с визуализациями
- 🕷️ Парсинг данных о достопримечательностях
- 🐳 Docker-развертывание

## Технологический стек

### Backend
- Django 4.2+
- Django REST Framework
- PostgreSQL
- Redis
- Celery
- Perplexity API
- BeautifulSoup4, Selenium
- Pandas, scikit-learn
- Matplotlib, Seaborn

### Frontend
- React 18+
- React Router
- Leaflet (карты)
- Axios
- Vite

### Infrastructure
- Docker & Docker Compose
- Nginx
- Gunicorn

## Установка и запуск

### С помощью Docker Compose (рекомендуется)

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd tourist-routes-service
```

2. Создайте файл `.env` на основе `.env.example`:
```bash
cp .env.example .env
```

3. Заполните необходимые переменные окружения в `.env`:
```env
SECRET_KEY=your-secret-key
DB_PASSWORD=your-db-password
PERPLEXITY_API_KEY=your-perplexity-api-key
PERPLEXITY_MODEL=sonar-pro
```

4. Запустите контейнеры:
```bash
docker-compose up -d
```

5. Примените миграции:
```bash
docker-compose exec backend python manage.py migrate
```

6. Создайте суперпользователя (опционально):
```bash
docker-compose exec backend python manage.py createsuperuser
```

7. Запустите парсинг достопримечательностей (опционально):
```bash
docker-compose exec backend python manage.py scrape_attractions
```

### Локальная установка

#### Backend

1. Создайте виртуальное окружение:
```bash
python3 -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows
```

2. Установите зависимости:
```bash
pip install -r requirements.txt
```

3. Настройте базу данных PostgreSQL и Redis

4. Создайте файл `.env` с настройками

5. Примените миграции:
```bash
cd backend
python manage.py migrate
```

6. Запустите сервер:
```bash
python manage.py runserver
```

#### Frontend

1. Установите зависимости:
```bash
cd frontend
npm install
```

2. Запустите dev-сервер:
```bash
npm run dev
```

## API Endpoints

### Аутентификация
- `POST /api/auth/register/` - Регистрация
- `POST /api/auth/login/` - Вход
- `POST /api/auth/refresh/` - Обновление токена
- `GET /api/auth/me/` - Текущий пользователь

### Достопримечательности
- `GET /api/attractions/` - Список достопримечательностей
- `GET /api/attractions/{id}/` - Детали достопримечательности
- `GET /api/attractions/nearby/` - Ближайшие достопримечательности

### Маршруты
- `GET /api/routes/` - Список маршрутов пользователя
- `POST /api/routes/` - Создание маршрута
- `GET /api/routes/{id}/` - Детали маршрута
- `POST /api/routes/generate/` - Генерация маршрута
- `POST /api/routes/{id}/optimize/` - Оптимизация маршрута

### Аналитика
- `GET /api/analytics/popular/` - Популярные маршруты
- `GET /api/analytics/stats/` - Статистика маршрутов
- `GET /api/analytics/charts/popularity/` - График популярности

## Генерация маршрутов

Система поддерживает три типа генерации маршрутов:

1. **LLM-генерация** - Использует Perplexity API для создания описательных маршрутов с актуальной информацией из веб-поиска
2. **Алгоритмическая** - Генерирует маршруты на основе фильтров и оптимизации расстояния
3. **Гибридная** - Комбинация LLM и алгоритмической оптимизации

### Настройка Perplexity API

Для использования генерации маршрутов через Perplexity необходимо:
1. Получить API ключ на [Perplexity AI](https://www.perplexity.ai/)
2. Установить переменную окружения `PERPLEXITY_API_KEY` в `.env` файле
3. Опционально настроить модель через `PERPLEXITY_MODEL` (по умолчанию: `sonar-pro`)

Perplexity также используется для обогащения данных о достопримечательностях, получаемых через веб-скрапинг.

## Тестирование

Запуск тестов:
```bash
cd backend
pytest
```

С покрытием кода:
```bash
pytest --cov=. --cov-report=html
```

## Структура проекта

```
tourist-routes-service/
├── backend/              # Django проект
│   ├── accounts/        # Аутентификация
│   ├── attractions/     # Достопримечательности
│   ├── routes/          # Маршруты
│   ├── scraper/         # Парсинг данных
│   ├── analytics/       # Аналитика
│   └── config/          # Настройки
├── frontend/            # React приложение
│   ├── src/
│   │   ├── components/
│   │   ├── pages/
│   │   └── services/
│   └── public/
├── docker-compose.yml
├── Dockerfile.backend
└── Dockerfile.frontend
```

## Лицензия

MIT
